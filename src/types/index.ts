export type Severity = 'High' | 'Moderate' | 'Low'
export type Diagnosis = { id:string; crop:string; disease:string; scientific:string; severity:Severity; confidence:number; image?:string; symptoms:string[]; actions:string[]; prevention:string; recovery:number; date:string }
export type Crop = { id:string; name:string; variety:string; stage:string; health:number; nextTask:string; color:string }
export type Hotspot = { id:string; district:string; state:string; disease:string; severity:Severity; reports:number; trend:number; position:[number,number] }
export type Pet = { id:string; name:string; type:string; breed?:string|null; createdAt?:string; updatedAt?:string }
export type CropChoice = 'Rice'|'Wheat'|'Maize'|'Cotton'|'Sugarcane'|'Tomato'|'Potato'|'Onion'|'Soybean'|'Chickpea'|'Groundnut'|'Mustard'|'Chilli'|'Grapes'|'Mango'|'Banana'|'Other'
export type UserProfile = { name:string; fullName?:string; phone?:string|null; profileImageUrl?:string|null; location:string; role:'farmer'|'home-grower'; language:string; selectedCrops?:CropChoice[]; pets?:Pet[]; onboardingCompleted:boolean }
export type CropCatalog = { id:string; name:string; scientificName:string|null; category:string|null; createdAt:string; updatedAt:string }
export type UserCrop = { id:string; userId:string; cropId:string|null; name:string; customName:string|null; variety:string; stage:string; plantedAt:string|null; area:number|null; areaUnit:string|null; notes:string|null; health:number; nextTask:string; color:string; createdAt:string; updatedAt:string }
export type DiagnosisSummary = { id:string; scanId:string; status:'pending'|'completed'|'failed'; availability:'unavailable'|'unsupported_crop'|'uncertain'|null; predictedCrop:string|null; predictedDisease:string|null; severity:string|null; confidence:number|null; modelName:string|null; modelVersion:string|null; createdAt:string; updatedAt:string }
export type CropTimelineEvent = { id:string; type:'crop_added'|'scan_created'|'diagnosis_pending'|'diagnosis_completed'|'diagnosis_failed'|'diagnosis_unavailable'; occurredAt:string; diagnosisId?:string; scanId?:string; status?:string; label:string }
export type UserCropDetail = { crop:UserCrop; catalog:CropCatalog|null; latestScan: { id:string; userId:string; cropId:string|null; userCropId:string|null; status:'pending'|'processing'|'completed'|'failed'; createdAt:string; updatedAt:string }|null; latestDiagnosis:DiagnosisSummary|null; diagnoses:DiagnosisSummary[]; timeline:CropTimelineEvent[] }
