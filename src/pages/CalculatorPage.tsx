import { Calculator } from 'lucide-react'; import { useState } from 'react'; import { Card, PageHeader } from '../components/ui/UI'; export function CalculatorPage(){const [area,setArea]=useState(100);const [dose,setDose]=useState(3);const total=(area*dose/100).toFixed(1);return <><PageHeader eyebrow="Simple field tools" title="Fertilizer Calculator"/><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><Card className="bg-forest text-white"><Calculator size={28}/><h2 className="mt-4 text-xl font-black">Mix with confidence</h2><p className="mt-2 text-sm leading-6 text-emerald-50">Estimate product quantity based on your plot size and the label’s recommended dose.</p><p className="mt-6 rounded-xl bg-white/10 p-3 text-xs text-emerald-100">Always follow the product label and local advisory. This is an estimate only.</p></Card><Card><label className="block text-sm font-bold">Growing area (m²)<input type="number" value={area} onChange={e=>setArea(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-ink/10 p-3 outline-forest"/></label><label className="mt-5 block text-sm font-bold">Label dose (g per 100 m²)<input type="number" value={dose} onChange={e=>setDose(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-ink/10 p-3 outline-forest"/></label><div className="mt-6 rounded-2xl bg-mint p-5"><p className="text-sm font-bold text-forest">Estimated quantity</p><p className="mt-1 text-4xl font-black text-forest">{total} g</p><p className="mt-1 text-sm text-forest/70">for {area} m² at {dose} g / 100 m²</p></div></Card></div></>}
