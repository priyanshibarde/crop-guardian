import { ArrowUpRight, Bell, Camera, CloudSun, MapPinned } from 'lucide-react'; import { Link } from 'react-router-dom'; import { alerts, hotspots } from '../data/mockData'; import { useLanguage } from '../i18n'; import { storage } from '../services/storageService'; import { Card, LinkArrow, PageHeader, Pill } from '../components/ui/UI';
export function HomePage(){const {t}=useLanguage();const crops=storage.crops();return <><PageHeader eyebrow="Your crop companion" title={t.greeting} action={<Link to="/alerts" className="relative rounded-full bg-white p-2.5 shadow-sm"><Bell size={19}/><i className="absolute right-2 top-2 h-2 w-2 rounded-full bg-coral"/></Link>}/><div className="grid gap-4 lg:grid-cols-[1.45fr_.85fr]"><Link to="/scan" className="relative overflow-hidden rounded-[28px] bg-forest p-6 text-white sm:p-8"><span className="absolute -right-8 -top-8 h-40 w-40 rounded-full border-[24px] border-emerald-300/20"/><p className="text-sm font-bold text-emerald-100">AI plant health check</p><h2 className="mt-2 max-w-sm text-3xl font-black leading-tight">A healthier crop starts with one photo.</h2><span className="mt-6 inline-flex items-center gap-2 rounded-xl bg-white px-4 py-3 text-sm font-bold text-forest"><Camera size={18}/>{t.scan}<ArrowUpRight size={16}/></span></Link><Card className="bg-sand"><div className="flex justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-white text-sun"><CloudSun/></span><Pill tone="amber">Nashik</Pill></div><h3 className="mt-5 text-lg font-extrabold">Humid day ahead</h3><p className="mt-1 text-sm text-ink/60">Avoid overhead watering. Check tomato leaves this evening.</p></Card></div><div className="mt-7 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-extrabold">{t.crops}</h2><Link to="/crops"><LinkArrow>View all</LinkArrow></Link></div><div className="grid gap-3 sm:grid-cols-3">{crops.map(c=><Card key={c.id}><div className={`grid h-11 w-11 place-items-center rounded-2xl ${c.color} text-xl`}>🌱</div><h3 className="mt-3 font-extrabold">{c.name}</h3><p className="text-xs text-ink/55">{c.variety}</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-slate-100"><i className="block h-full rounded-full bg-forest" style={{width:`${c.health}%`}}/></div><p className="mt-1 text-xs font-bold text-forest">{c.health}% healthy</p></Card>)}</div></div><div><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-extrabold">{t.warning}</h2><Link to="/hotspots"><LinkArrow>Map</LinkArrow></Link></div><Card><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-red-50 text-red-500"><MapPinned/></span><div><Pill tone="red">High activity</Pill><p className="mt-1 text-sm font-bold">{hotspots[0].disease}</p></div></div><p className="mt-3 text-sm text-ink/60">{hotspots[0].reports} reports around {hotspots[0].district}. Protect your tomatoes early.</p></Card></div></div><div className="mt-7"><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-extrabold">Today’s alerts</h2><Link to="/alerts"><LinkArrow>See all</LinkArrow></Link></div><div className="grid gap-3 md:grid-cols-3">{alerts.map(a=><Card key={a.id} className="border border-emerald-950/5"><div className="flex justify-between"><Pill tone={a.severity==='High'?'red':a.severity==='Moderate'?'amber':'green'}>{a.severity}</Pill><span className="text-xs text-ink/40">{a.time}</span></div><p className="mt-3 font-bold">{a.title}</p><p className="mt-1 text-sm text-ink/55">{a.body}</p></Card>)}</div></div></>}
